# Core dependencies for The Goldbach--Chen Frontier (reproducibility artifact).
# Tested with Python 3.10.
numpy==2.2.6
scipy==1.15.3
matplotlib==3.10.9
sympy==1.14.0
pandas==2.3.3

# Optional: only for the selection-MIP driver (experiments/run_mip.py).
# Requires a Gurobi license; everything else runs without it.
# gurobipy==11.0.0
